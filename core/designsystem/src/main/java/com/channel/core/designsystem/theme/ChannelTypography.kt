package com.channel.core.designsystem.theme

import androidx.compose.material3.Typography

/** Material3's default type scale — swap in a brand FontFamily here once one exists. */
internal val ChannelTypography: Typography = Typography()
